{% extends 'base.html.twig' %}

{% block title %}Product{% endblock %}

{% block body %}
    <h1>Product</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id_Product</th>
                <td>{{ product.idProduct }}</td>
            </tr>
            <tr>
                <th>Price</th>
                <td>{{ product.price }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ product.description }}</td>
            </tr>
            <tr>
                <th>Image</th>
                <td>{{ product.image }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ product.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_productfk_index') }}">back to list</a>

    <a href="{{ path('app_productfk_edit', {'id_Product': product.id_Product}) }}">edit</a>

    {{ include('productfk/_delete_form.html.twig') }}
{% endblock %}
