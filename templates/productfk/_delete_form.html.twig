<form method="post" action="{{ path('app_productfk_delete', {'id_Product': product.id_Product}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ product.id_Product) }}">
    <button class="btn">Delete</button>
</form>
