{% extends 'base.html.twig' %}

{% block title %}Categorie index{% endblock %}

{% block body %}
    <h1>Categorie index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Idcategorie_id</th>
                <th>Nomcategorie</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for categorie in categories %}
            <tr>
                <td>{{ categorie.idcategorieId }}</td>
                <td>{{ categorie.NomCategorie }}</td>
                <td>
                    <a href="{{ path('app_categorie_show', {'idcategorie_id': categorie.idcategorieId}) }}">show</a>
                    <a href="{{ path('app_categorie_edit', {'idcategorie_id': categorie.idcategorieId}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_categorie_new') }}">Create new</a>
{% endblock %}
